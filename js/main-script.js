// 'use strict';

// (function($, sr) {
//     jQuery.fn[sr] = function(fn) {
//         return fn ? this.bind('resize', debounce(fn)) : this.trigger(sr);
//     };
// })
// (jQuery, 'smartresize');

// $(function() {
//     //Fix main section height
//     let setMainBanner = function() {
//         let mainHeight = $(window).height();

//         $('#overlay').height(mainHeight);
//     }
//     setMainBanner();
// });
